At first I compute the histograms for my all images (they all do not have standard behaviour) using the same code of Task1, the histograms can be found in the same xlsx in Task1. 
Then we match it with benchmark histogram, using the code attached here. And after it calibrate.
